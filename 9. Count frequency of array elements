arr = [1, 2, 2, 3, 4, 4, 4, 5]

freq = {}

for num in arr:
    if num in freq:
        freq[num] += 1
    else:
        freq[num] = 1

# Print frequencies
for key in freq:
    print(f"{key} occurs {freq[key]} times")
